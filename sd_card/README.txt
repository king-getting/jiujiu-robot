啾啾 SD 卡文件说明
====================

phrases.txt
- 每行一句鼓励语，UTF-8 编码。
- 固件启动时会读取这个文件；没有此文件时使用内置 20 句。
- 当前固件只显示与内置字模完全相同的句子；新增任意中文需要先扩展中文字模。

gougou.bmp
- 内置狗头图片备份。
- 当前固件默认使用内置图，不读这个文件；以后需要换图时可打开 USE_SD_IMAGE。

sensor.csv
- 固件运行后自动创建，每 60 秒追加一行：timestamp_ms,temp,humi,air,dht_ok
- 不需要手动创建。